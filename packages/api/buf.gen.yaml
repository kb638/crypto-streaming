# packages/api/buf.gen.yaml
version: v2
plugins:
  - local: protoc-gen-es
    out: gen
    opt:
      - target=ts
      # add ".js" to ESM imports so NodeNext & Next.js are happy
      - import_extension=js
